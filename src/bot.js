import { ActionRowBuilder, ButtonBuilder, ButtonStyle, Client, EmbedBuilder, Events, GatewayIntentBits, MessageFlags, PermissionFlagsBits, REST, Routes, SlashCommandBuilder, StringSelectMenuBuilder } from 'discord.js';
import { initDatabase } from './database.js';
import { GAME, SPECIES } from './config.js';
import { activePet, adopt, balanceChange, buy, care, claimDaily, equip, getUser, petById, petsOf, rename, rewardMessage, setActive, unequip } from './game.js';
import { dressingHome, dressingItem, petPayload, pinkHomePayload, shopItemPayload, welcomePayload } from './ui.js';

const commands=[
 new SlashCommandBuilder().setName('commencer').setDescription('Créer ton profil BalekPets et choisir ton premier animal.'),
 new SlashCommandBuilder().setName('animal').setDescription('Afficher et gérer ton animal actif.'),
 new SlashCommandBuilder().setName('adopter').setDescription('Ouvrir l’élevage de Julien & Miky.'),
 new SlashCommandBuilder().setName('elevage').setDescription('Ouvrir l’élevage de Julien & Miky.'),
 new SlashCommandBuilder().setName('pink').setDescription('Ouvrir la boutique Pink Studio.'),
 new SlashCommandBuilder().setName('pink-studio').setDescription('Ouvrir la boutique Pink Studio.'),
 new SlashCommandBuilder().setName('journalier').setDescription('Récupérer 200 Balekium.'),
 new SlashCommandBuilder().setName('profil').setDescription('Voir un profil BalekPets.').addUserOption(o=>o.setName('membre').setDescription('Membre à consulter')),
 new SlashCommandBuilder().setName('renommer').setDescription('Renommer ton animal actif.').addStringOption(o=>o.setName('nom').setDescription('Nouveau nom').setRequired(true).setMinLength(2).setMaxLength(24)),
 new SlashCommandBuilder().setName('balekium').setDescription('Afficher ton solde.'),
 new SlashCommandBuilder().setName('admin-balekium').setDescription('Définir le solde d’un membre.').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addUserOption(o=>o.setName('membre').setDescription('Membre').setRequired(true)).addIntegerOption(o=>o.setName('montant').setDescription('Nouveau solde').setRequired(true).setMinValue(0))
].map(c=>c.toJSON());

async function deploy(){ const token=process.env.DISCORD_TOKEN,id=process.env.CLIENT_ID,guild=process.env.GUILD_ID; if(!id||!guild)return console.warn('[BalekPets] CLIENT_ID ou GUILD_ID absent, commandes non redéployées.'); await new REST({version:'10'}).setToken(token).put(Routes.applicationGuildCommands(id,guild),{body:commands}); console.log(`[BalekPets] ${commands.length} commandes déployées.`); }
const replace=(p)=>({...p,attachments:[]});
const ephemeral=(content)=>({content,flags:MessageFlags.Ephemeral});
function nursery(){ const e=new EmbedBuilder().setColor(0x8fd09d).setTitle('🌿 Élevage de Julien & Miky').setDescription('Adopte un nouveau compagnon. Le premier animal doit être choisi avec `/commencer`.').addFields(Object.values(SPECIES).map(s=>({name:`${s.emoji} ${s.name}`,value:s.unavailable?'Bientôt disponible':`${s.price} ${GAME.currency} · ${s.description}`,inline:false}))); const menu=new StringSelectMenuBuilder().setCustomId('nursery').setPlaceholder('Choisir un animal à adopter').addOptions(Object.values(SPECIES).map(s=>({label:s.name,value:s.id,emoji:s.emoji,description:s.unavailable?'Bientôt disponible':`${s.price} ${GAME.currency}`,default:false}))); return {embeds:[e],components:[new ActionRowBuilder().addComponents(menu)]}; }
export async function startBot(){ const token=process.env.DISCORD_TOKEN;if(!token)throw new Error('DISCORD_TOKEN absent du fichier .env'); initDatabase(process.env.DATABASE_PATH??'./data/balekpets.sqlite'); await deploy(); const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMessages]}); client.once(Events.ClientReady,c=>console.log(`[BalekPets] Connecté en tant que ${c.user.tag}`)); client.on(Events.MessageCreate,m=>{if(m.inGuild()&&!m.author.bot)rewardMessage(m.author.id);}); client.on(Events.InteractionCreate,async i=>{try{if(i.isChatInputCommand())await command(i);else if(i.isStringSelectMenu())await select(i);else if(i.isButton())await button(i);}catch(e){console.error('[BalekPets] Interaction:',e);const p=ephemeral('Une anomalie a traversé la Zone du Silence. Regarde la console.');if(i.replied||i.deferred)await i.followUp(p).catch(()=>{});else await i.reply(p).catch(()=>{});}}); await client.login(token); }
async function command(i){ const id=i.user.id; if(i.commandName==='commencer'){if(petsOf(id).length)return i.reply(ephemeral('Ton élevage existe déjà. Utilise `/animal`.'));return i.reply({...welcomePayload(),flags:MessageFlags.Ephemeral});} if(i.commandName==='animal'){const p=activePet(id);return p?i.reply({...await petPayload(p,i.member??i.user),flags:MessageFlags.Ephemeral}):i.reply(ephemeral('Utilise d’abord `/commencer`.'));} if(['adopter','elevage'].includes(i.commandName))return i.reply({...nursery(),flags:MessageFlags.Ephemeral}); if(['pink','pink-studio'].includes(i.commandName))return i.reply({...pinkHomePayload(),flags:MessageFlags.Ephemeral}); if(i.commandName==='journalier'){const r=claimDaily(id);return i.reply(ephemeral(r.ok?`✅ 200 Balekium reçus. Solde : **${r.balance}**.`:`Reviens dans **${Math.ceil(r.remaining/3600000)} h**.`));} if(i.commandName==='balekium')return i.reply(ephemeral(`Tu possèdes **${getUser(id).balance} Balekium**.`)); if(i.commandName==='renommer'){const name=i.options.getString('nom',true).trim();return i.reply(ephemeral(rename(id,name)?`Ton animal s’appelle maintenant **${name}**.`:'Aucun animal actif.'));} if(i.commandName==='profil'){const u=i.options.getUser('membre')??i.user,ps=petsOf(u.id),bal=getUser(u.id).balance;const e=new EmbedBuilder().setColor(0xb68cff).setTitle(`Profil BalekPets de ${u.displayName}`).setThumbnail(u.displayAvatarURL()).addFields({name:'Balekium',value:String(bal),inline:true},{name:'Animaux',value:String(ps.length),inline:true},{name:'Collection',value:ps.length?ps.map(p=>`• **${p.name}** · ${SPECIES[p.species_id]?.name} · niveau ${p.level}`).join('\n'):'Aucun animal.'});return i.reply({embeds:[e]});} if(i.commandName==='admin-balekium'){const u=i.options.getUser('membre',true),amount=i.options.getInteger('montant',true),current=getUser(u.id).balance;balanceChange(u.id,amount-current,`admin:${i.user.id}`);return i.reply(ephemeral(`Solde de ${u} défini à **${amount} Balekium**.`));}}
async function select(i){const id=i.user.id;if(i.customId==='starter'){const r=adopt(id,i.values[0],true);return i.update({content:r.ok?`🎉 **${r.pet.name}** rejoint ton élevage. Utilise maintenant "/animal".`:'Adoption impossible.',embeds:[],components:[]});} if(i.customId==='pink_species')return i.update({...pinkHomePayload(i.values[0]),attachments:[]}); if(i.customId==='nursery'){const sp=SPECIES[i.values[0]];const e=new EmbedBuilder().setColor(0x8fd09d).setTitle(`${sp.emoji} ${sp.name}`).setDescription(sp.description).addFields({name:'Prix',value:sp.unavailable?'Bientôt disponible':`${sp.price} Balekium`});const b=new ButtonBuilder().setCustomId(`adopt:${sp.id}`).setLabel(sp.unavailable?'Indisponible':`Adopter · ${sp.price}`).setDisabled(Boolean(sp.unavailable)).setStyle(ButtonStyle.Success);return i.update({embeds:[e],components:[new ActionRowBuilder().addComponents(b)]});} if(i.customId==='switchpet'){setActive(id,Number(i.values[0]));const p=activePet(id);return i.update(replace(await petPayload(p,i.member??i.user)));}}
async function button(i){const [a,...p]=i.customId.split(':'),id=i.user.id;if(a==='pink_home')return i.update({...pinkHomePayload(),attachments:[]});if(a==='pink')return i.update({...pinkHomePayload(p[0]),attachments:[]});if(a==='shop')return i.update(replace(await shopItemPayload(id,p[0],p[1],Number(p[2]))));if(a==='buy'){const r=buy(id,p[3]);const msg=r.ok?`✅ **${r.item.name}** acheté. Solde : **${r.balance}**.`:r.reason==='funds'?'❌ Pas assez de Balekium.':'Tu possèdes déjà cet article.';return i.update(replace(await shopItemPayload(id,p[0],p[1],Number(p[2]),msg)));}if(a==='adopt'){const r=adopt(id,p[0],false);const msg={owned:'Tu possèdes déjà cette espèce.',funds:'Pas assez de Balekium.',start_first:'Utilise d’abord `/commencer`.',unavailable:'Cette espèce arrive bientôt.'}[r.reason];return r.ok?i.update({content:`🎉 **${r.pet.name}** rejoint ta collection. Solde : **${r.balance}**.`,embeds:[],components:[]}):i.reply(ephemeral(msg??'Adoption impossible.'));}if(a==='care'){const r=care(id,Number(p[1]),p[0],false);if(!r.ok)return i.reply(ephemeral(r.reason==='cooldown'?`Action disponible dans ${Math.ceil(r.remaining/60000)} min.`:r.reason==='full'?'Cette jauge est déjà pleine.':'Action impossible.'));return i.update(replace(await petPayload(r.pet,i.member??i.user,'✅ Soin effectué, +5 XP.')));}if(a==='express'){const pet=petById(Number(p[0]));const row=new ActionRowBuilder().addComponents(['food','happiness','vitality'].map(t=>new ButtonBuilder().setCustomId(`expresscare:${t}:${pet.id}`).setLabel(`${t==='food'?'Nourriture':t==='happiness'?'Bonheur':'Vitalité'} +10 · 50`).setStyle(ButtonStyle.Secondary)));return i.reply({content:'Soins express sans délai :',components:[row],flags:MessageFlags.Ephemeral});}if(a==='expresscare'){const r=care(id,Number(p[1]),p[0],true);return i.reply(ephemeral(r.ok?`✅ Soin express effectué. Solde : **${r.balance}**.`:r.reason==='funds'?'Pas assez de Balekium.':'Action impossible.'));}if(a==='dress'){const pet=petById(Number(p[0]));if(!pet||pet.user_id!==id)return i.reply(ephemeral('Ce dressing ne t’appartient pas.'));return i.update?i.update({...dressingHome(pet),attachments:[]}):i.reply({...dressingHome(pet),flags:MessageFlags.Ephemeral});}if(a==='dressslot'){const pet=petById(Number(p[0]));return i.update(replace(await dressingItem(id,pet,p[1],Number(p[2]))));}if(a==='equip'){const petId=Number(p[0]),slot=p[1],index=Number(p[2]),itemId=p[3];const r=itemId==='none'?unequip(id,petId,slot):equip(id,petId,itemId);return i.update(replace(await dressingItem(id,r.pet??petById(petId),slot,index,r.ok?'✅ Apparence mise à jour.':'Modification impossible.')));}if(a==='switch'){const ps=petsOf(id);if(ps.length<2)return i.reply(ephemeral('Tu ne possèdes qu’un seul animal.'));const menu=new StringSelectMenuBuilder().setCustomId('switchpet').setPlaceholder('Choisir l’animal actif').addOptions(ps.map(x=>({label:`${x.name} · ${SPECIES[x.species_id].name}`,value:String(x.id),default:Boolean(x.is_active)})));return i.reply({content:'Choisis ton animal actif :',components:[new ActionRowBuilder().addComponents(menu)],flags:MessageFlags.Ephemeral});}}
